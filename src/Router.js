import { Suspense } from "react";
import { Route, Routes } from "react-router";

export default function Router() {
    return (
        <Suspense>
            <Routes>
            </Routes>
        </Suspense>
    );
}